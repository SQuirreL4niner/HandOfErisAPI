package com.handoferis.security;

import com.handoferis.blob.AzureProperties;
import com.microsoft.azure.keyvault.KeyVaultClient;

public class AzureConfig {
//    private KeyVaultClient buildAzureIntegrationClient() {
//        var azureProperties = new AzureProperties();
//        var applicationId = azureProperties.getApplicationId();
//        var clientSecret = azureProperties.getClientSecret();
//        var azureCreds = new KeyVaultClientCredentials(applicationId, clientSecret);
//        return new KeyVaultClient(azureCreds);
//    }
}